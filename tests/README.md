# Tests

Unit and integration tests for pipeline transformations (Bronze/Silver/Gold) and the serving API.

Currently, API endpoint testing lives in `test_api.py` at the project root.
Future unit tests for individual pipeline scripts (ingestion, cleaning, joins) will live here.
