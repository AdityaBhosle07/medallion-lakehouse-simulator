# Config

Configuration files for pipeline settings (file paths, thresholds, environment-specific overrides).

Status: scaffolding in place, configuration values to be externalized here from pipeline scripts.
